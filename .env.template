# ============================================================
# TTS 语音功能环境变量模板
# 本地测试:复制为 `.env`(已 gitignore)并填入真实值。
# 服务器(Render):这四个变量同样要在 Dashboard 里配置,speak MCP
#   工具才可用(SUPABASE 两个如已配过 phone_activity 则不用重复配)。
# ombre 服务本身的其他变量见 .env.example。
# ============================================================

ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
